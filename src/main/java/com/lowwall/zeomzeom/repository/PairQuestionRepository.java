package com.lowwall.zeomzeom.repository;

import com.lowwall.zeomzeom.entity.PairQuestion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PairQuestionRepository extends JpaRepository<PairQuestion, Long> {
}
